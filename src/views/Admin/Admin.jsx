import React from 'react';
import { Link } from 'react-router-dom';

import './Admin.scss';

import BaseButton from '../../components/BaseButton/BaseButton';

const Admin = () => {
  const text = 'Reported users and comments';
  return (
    
  );
};

export default Admin;
